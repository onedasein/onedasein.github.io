#import "lib.typ": *

= 总览：怎么用这份路线图

#note[
  起点：2026-09（大二上，约 2029 届毕业）｜时间：每周几乎全部可自由实践
  目标：三个方向——① 后端/基础架构 ② AI Infra/高性能计算 ③ 机器学习/Agent，并补齐 CSAPP 等系统基础。
]

== 0. 一句话策略

*大二上半年的任务不是「选方向」，而是「用作品收集证据」。*
三个方向共用一条系统底座（C → CSAPP → OS/网络/并发），所以先修底座不会浪费；
方向用一个 6 周的「轮转公测」来决定，用评分表决定，不用感觉决定。

```
                        ┌──→ ① 后端 · 基础架构
C / CSAPP / OS / 网络 ──┤
        并发 / 存储     └──→ ② AI Infra · HPC（= 系统功底 ∩ ML）
                                   ↑
        数学 + 动手训练 ────────────┘
                    └──→ ③ ML / Agent（应用层，可随时切入）
```

*三条铁律*

+ 任何时刻只有 *1 条主线 + 1 条副线*，外加每天 30 分钟底座。
+ 里程碑必须是*可运行的产出*（repo + benchmark + writeup），不是「看完某课程」。
+ *实习是唯一的硬 deadline*，所有规划倒推自它（见下表）。

== 1. 硬性时间锚点（倒推规划）

#tbl(
  (auto, auto, 1.6fr, 1.4fr),
  [时间], [学期], [唯一关键目标], [验收],
  [2026.9 – 2027.1], [大二上], [底座 + 三个方向轮转探针], [C+CSAPP 4 个 lab、epoll server、3 个探针 repo],
  [2027.2 – 2027.7], [大二下], [*定主攻方向* + 第一个像样的项目 + 投第一份日常实习], [决策备忘、一个可讲的项目、面试能过简历关],
  [2027.9 – 2028.1], [大三上], [主攻方向深挖 + *大厂日常实习*], [第二/第三个项目、一段实习经历],
  [2027.12 – 2028.4], [—], [*2028 暑期实习投递*（最重要的一次，有转正通道）], [拿到 offer],
  [2028.7 – 2028.9], [大三暑假], [暑期实习，争取转正（return offer）], [转正 or 一份大厂背书],
  [2028.8 – 2028.11], [大四上], [2029 届秋招], [上岸],
)

#note[
  时间充裕的人最容易犯的错：把「准备」无限期延长到秋招前。*从大二下开始就投简历*，用面试反馈校准学习方向，比闭门造车快 10 倍。
]

== 2. 文档地图

#tbl(
  (auto, 2fr, 1fr),
  [文件], [内容], [什么时候看],
  [00 方向选择与轮转公测], [*先读这个*：三个探针项目 + 评分表 + 决策规则], [现在],
  [01 共同底座-CSAPP与系统], [C、CSAPP 读法与 lab、OSTEP、CS144、15-445、6.824], [现在，持续 12 个月],
  [02 主线A-后端与基础架构], [后端方向的技能阶梯与项目], [探针期 / 定方向后],
  [03 主线B-AI-Infra与HPC], [CUDA/Triton、分布式训练、推理系统], [探针期 / 定方向后],
  [04 主线C-机器学习与Agent], [从零训练、对齐、Agent 系统与评测], [探针期 / 定方向后],
  [05 周计划与里程碑], [分学期目标、每周节奏模板、验收 gate], [每学期初],
  [06 资源与避坑], [书/课/源码/工具清单 + 12 条避坑], [随时查],
  [progress], [可勾选进度表], [每周五更新],
)

== 3. 每周节奏模板（时间充裕版，约 25–30h）

#tbl(
  (auto, 1.9fr, auto),
  [时段], [内容], [小时],
  [每天固定 0.5h], [底座：CSAPP 精读 / 算法题 / 英语技术文档], [3.5],
  [主线（大块时间 ×3–4 次）], [写代码、啃 lab、调性能], [14],
  [副线（大块时间 ×1–2 次）], [另一方向的探索性学习], [5],
  [周五], [写 writeup（500–1500 字）+ 更新 progress.md + 推 commit], [2],
  [周日], [复盘：本周卡点、下周计划（写进文件，不要放脑子里）], [1],
)

*每周必须留下三样东西*：≥5 个 commit、1 篇 writeup、1 个可运行的新东西。
这是「时间充裕」唯一不会翻车的使用方式。

== 4. 关于「三个方向不知道选哪个」

这是*正常的、甚至是正确*的状态——大二不该凭想象押注。请直接去读 00 方向选择与轮转公测，
用 6 周、三个 30–50 小时的小项目，把「喜欢」变成数据。三个方向的技能在系统层高度重叠，
所以这段时间即使最后转向，投入也不会作废。

*额外的现实信息*（可能影响你的选择）：

- *① 后端/基础架构*：岗位量最大、最稳，天花板高（存储/数据库/分布式中间件），但纯 CRUD 岗正在被压缩，要做就做「造轮子」的那一侧。
- *② AI Infra/HPC*：当前最稀缺、溢价最高，门槛也最高（要真会 C/CUDA 和体系结构），本科生能拿出手的人极少。
- *③ ML/Agent*：岗位多但竞争最卷，且*纯应用层容易被上层框架变化清洗*；真正抗跌的是「能把模型训出来/训得快/评测得住」的人。
- 三者不是互斥：*②=①+ML*，现实岗位多是组合（如「推理引擎工程师」「训练框架工程师」「Agent 平台工程师」）。
  最稳的路径是 *A（打底）→ B（差异化）*，C 作为随时可切入的副线。
